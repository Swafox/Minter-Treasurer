'use strict';

/**
 * @param {TxParams} params
 * @return {TxParams}
 */
function decorateAddLiquidityTxParams(params) {
  if (typeof params.gasCoin === 'undefined') {
    params.gasCoin = params.data.coin0;
  }

  return params;
}

module.exports = decorateAddLiquidityTxParams;
