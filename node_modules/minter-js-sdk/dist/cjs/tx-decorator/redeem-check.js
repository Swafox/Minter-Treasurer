'use strict';

var check = require('../check.js');
var redeemCheck = require('../tx-data/redeem-check.js');

/**
 * @param {TxParams} params
 * @return {TxParams}
 */

function decorateRedeemCheckTxParams(params) {
  var check$1 = params.data.check || redeemCheck.fromRlp(params.data).check;
  params.gasCoin = check.getGasCoinFromCheck(check$1); // only gasPrice: 1 is allowed by blockchain

  params.gasPrice = 1;
  return params;
}

module.exports = decorateRedeemCheckTxParams;
