'use strict';

/**
 * @param {TxParams} params
 * @return {TxParams}
 */
function decorateSellTxParams(params) {
  // force gasCoin to be same as coinToSell
  params.gasCoin = params.data.coinToSell;
  return params;
}

module.exports = decorateSellTxParams;
